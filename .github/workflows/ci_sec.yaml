on:
  push:
    branches:
      - main

jobs:
  secret:
    runs-on: ubuntu-latest

    steps:
      - name: Use Secret Safely
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: echo "Length of key: ${#API_KEY}"