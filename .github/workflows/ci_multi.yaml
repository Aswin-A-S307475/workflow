on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hello from Build Stage" > app.txt
          echo "File created successfully"

    test:
      needs: build
      runs-on: ubuntu-latest

      steps:
        - run: echo "Test job"