on: [push]

jobs:
  node:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout"
        uses: actions/checkout@v4

      - name: "Node Setup"
        uses: actions/setup-node@v4
        with:
          node-version: 18
      
      - run: npm install
      - run: npm test 